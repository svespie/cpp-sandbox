// preprocessor directive that includes header iostream
#include <iostream>

// start of program
int main()
{
    /* write to the console / screen */
    std::cout << "hello, world!" << std::endl;

    // return a value of 0 to the OS
    return 0;
}